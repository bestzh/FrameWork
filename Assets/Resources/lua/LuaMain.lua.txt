-- Lua主脚本
-- 游戏启动时首先加载此脚本

print("[LuaMain] Lua主脚本已加载")

UIHelper = require("ui_helper")
ResHelper = require("res_helper")
PoolHelper = require("pool_helper")
SceneHelper = require("scene_helper")
AudioHelper = require("audio_helper")
SaveHelper = require("save_helper")
EventHelper = require("event_helper")
Table = CS.Table
UnityEngine = CS.UnityEngine

require("game.gamestart")

function Start()

end
